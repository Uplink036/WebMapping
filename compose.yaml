services:
  crawler:
    build: containers/crawler
    environment:
      - SCRUB_DB=1
    volumes:
      - ./logs:/logs
    depends_on:
      - database
  database:
    ports:
      - 27017:27017
    image: "mongodb/mongodb-community-server"