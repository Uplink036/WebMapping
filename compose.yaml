services:
  web:
    build: . 
    environment:
      - CONN_STR=mongodb://user:pass@mongodb
    volumes:
      - ./logs:/logs
    depends_on:
      - database
  database:
    ports:
      - 27017:27017
    image: "mongodb/mongodb-community-server"